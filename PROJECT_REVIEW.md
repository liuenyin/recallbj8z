# 项目审查与修复报告

审查日期：2026-09-08。

项目为 React 19 + TypeScript + Vite 的浏览器高中生活模拟游戏。主要业务集中在 `hooks/useGameLogic.ts` 和 `App.tsx`，事件、竞赛、周计划和数值规则位于 `data/`。游戏及账号数据保存在 localStorage，AI 事件通过用户配置的兼容接口请求。

## 已确认并修复

| 问题 | 原来的后果 | 本次修改 |
| --- | --- | --- |
| 账号写入异常被吞掉 | 存储已满时仍显示创建或切换成功 | 写入失败抛出可读错误，弹窗展示失败，创建中途失败回滚新增档案 |
| 删除当前账号后才判断当前账号 | 读取函数已回退，持久化的当前账号仍指向已删除账号 | 删除前记录并切换到游客账号 |
| 账号列表缺少去重和游客兜底 | 重复 ID、空档案或缺失游客造成列表与选择不一致 | 过滤异常记录、按 ID 去重、始终保留游客 |
| AI 配置保存失败仍关窗 | 配置只在内存生效，刷新后丢失却没有提示 | 保存成功后更新配置，失败保留弹窗并显示错误 |
| AI 有效事件少于要求仍被接收 | 只有一个有效事件时未触发离线回退 | 少于两个有效事件时拒绝结果，沿用现有回退流程 |
| 存档可携带异常成绩单、班级、世界信息等 | `scores: null` 等可导致渲染崩溃或错误资源路径 | 独立校验成绩、世界信息、剧情标记、计数和界面字段；重算成绩合计 |
| 存档直接信任天赋的展示数据 | 异常天赋名称可导致渲染错误，旧定义无法更新 | 根据有效 ID 从当前天赋定义恢复并去重 |
| 无法读取的存档直接删除 | 丢失原始数据，难以人工恢复 | 先在原键名加 `_corrupt_backup` 保存原始内容，再移除无效存档；备份失败时保留原存档 |
| 样式、图标、字体依赖外部 CDN | 无外网时界面缺少样式和图标 | Tailwind 在构建期生成，Font Awesome 本地打包，使用系统中文字体 |
| 效率进度条用 20 作为视觉满值 | 效率 20 时已满，与 30 的范围不一致 | 按 30 计算百分比 |
| 手机首页标题挤出孤立尾字 | 窄屏标题断行不自然 | 调整移动端图标大小、间距和标题字号 |
| favicon 使用网站根路径 | 子目录部署可能无法读取图标 | 改用相对路径 |
| 缺少显式 React 类型依赖和统一检查入口 | 安装环境间类型检查能力不一致，容易遗漏检查 | 添加 React 类型、Node 版本要求和 `npm run check` |
| 依赖审计报告 3 项已知漏洞 | 构建依赖存在数据库报告的问题 | 在兼容范围内更新锁定版本，更新后审计为 0 项 |

## 验证证据与边界

- 类型检查、全部 51 项测试与生产构建通过。
- 新增 6 项实际执行的持久化测试，覆盖异常账号、账号隔离、写入失败、创建回滚、成绩单和城市数据恢复。
- 新增可重复执行的浏览器验证：屏蔽外部 HTTP 请求后检查样式和图标、390 像素移动端溢出、创建账号、新游戏与首个事件结算、异常可选存档字段恢复，以及模拟 AI 响应的数量和数值限制、配置写入失败。
- 浏览器测试使用独立临时档案，不读取或修改真实玩家浏览器档案。截图输出到被 Git 忽略的 `artifacts/`。
- 已查看移动端首页和桌面游戏截图。浏览器验证没有捕获到页面运行时异常。
- 依赖审计结果是执行时数据库的结果，不代表不存在尚未公开的问题。
- 没有真实调用付费 AI 服务；AI 接口验证使用模拟响应。
- 尚未穷举所有随机事件、四档难度和 OI/MO 路线的完整学年组合，也未覆盖 Safari、Firefox 和真实手机。这次修复不能证明“所有问题已经清零”。

## 后续改进方向（按优先级）

1. **把游戏状态迁移做成可测试的独立模块。** 当前主逻辑约 1,800 行，同时负责流程、存档、异步 AI 和结算；逐步拆为纯函数与明确的阶段状态机，先保持行为一致，再增加功能。
2. **补完整路线的行为测试。** 现有测试中不少只是匹配源码字符串，应逐步替换为操作与结果断言。优先覆盖普通/OI/MO 完整学年、考试返回、选科、存读档、周计划、连点和 AI 取消；注入可设种子的随机数以稳定复现。
3. **分批加载事件与界面。** 生产主脚本目前约 1.07 MB（gzip 约 346 KB），构建仍提示体积较大。可按学期和路线加载事件数据，延迟加载竞赛历史与说明弹窗；用首屏耗时验证收益，避免仅拆分文件掩盖警告。
4. **完善存档生命周期。** 增加带版本的迁移、导入导出、多个备份槽、明确的恢复提示。现在已保留最近的损坏存档原文，但还没有供用户操作的恢复界面。
5. **完善可访问性。** 为通用弹窗补齐对话框语义、键盘焦点限制和返回、Escape 关闭、图标按钮名称；将时间表中的可点击 div 改成可键盘操作的控件。
6. **集中维护规则。** 周计划界面和执行逻辑存在重复的次数/可用时段规则，建议共用无副作用的计划校验结果，减少以后修改一处而漏改另一处。
7. **按产品需求决定 AI 与账号后端。** 当前账号只是本机档案，API Key 保存在浏览器。公开多人使用或需要跨设备同步时，应设计服务端认证、额度限制与密钥管理；本地单机用途可以继续沿用当前模式。
8. **整理历史工具与产品信息。** `legacy_scripts/` 留有大量一次性补丁，建议标明仅供考古、禁止作为常规升级流程；更新日志、帮助文案及实际规则应建立统一来源。赞助头像等非核心外部内容仍依赖网络。

## 复查命令

```text
npm ci
npm run check
npm run dev
# 另一个终端，在开发服务运行时：
npm run test:browser
```

浏览器位置可通过 `CHROME_PATH` 指定；浏览器测试默认访问 `http://127.0.0.1:3000/`，可用 `SMOKE_URL` 修改。

## 后续轻量调整

按“小项目保持简单”的方向，已合并周计划预览与执行的单次收益计算，修正重复活动兴奋值预览偏差；调整周计划布局并加入清空/恢复；统一阶段日期中文名称；新增校园手记文字导出；删除首页固定显示正常的“运行状态”装饰。沿用现有组件与依赖。
